{
  "name": "solar-explorer-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK stack for hosting the Solar Explorer frontend (S3 + CloudFront + ACM + Route 53).",
  "bin": {
    "solar-explorer": "bin/solar-explorer.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "bootstrap": "cdk bootstrap"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.1118.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.3.0",
    "dotenv": "^16.4.5"
  }
}
